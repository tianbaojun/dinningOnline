<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tabjin.administrator.dinningonline">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".HomePageActivity"/>
        <activity android:name=".AllfoodActivity"/>
        <activity android:name=".AllfoodItemsActivity"/>
        <activity android:name=".CartActivity"/>
        <activity android:name=".HistoryOrderActivity"/>
        <activity android:name=".OrderActivity"/>
        <activity android:name=".TradingOrderActivity"/>
        <activity android:name=".Login"/>
        <activity android:name=".Register"/>

    </application>

</manifest>
